language: node_js
node_js:
  - "10"

sudo: required

services:
  - docker

script:
- docker run --privileged -v "$(pwd)":/sitespeed.io sitespeedio/sitespeed.io --budget.configPath myBudget.json -n 1 http://cabb32ce1179.ngrok.io
# - yarn run size

after_success:
  - yarn run lh -- --perf=90 http://cabb32ce1179.ngrok.io

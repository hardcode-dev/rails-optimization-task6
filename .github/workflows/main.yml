name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: checkout code
      uses: actions/checkout@v1
    - name: run sitespeedio test
      uses: docker://sitespeedio/sitespeed.io:12.1.0-action
      with:
        args: https://758263761.ngrok.io -n 1 --budget.configPath /github/workspace/budget.json

name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v1
      - name: run sitespeedio test
        uses: docker://sitespeedio/sitespeed.io:latest
        with:
          args: http://___.ngrok.io/ -n 1 --budget.configPath /github/workspace/config/budget.json
